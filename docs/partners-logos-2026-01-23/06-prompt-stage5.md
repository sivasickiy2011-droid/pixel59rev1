# Промпт: Этап 5 - Финальная проверка и тестирование

**Этап:** 5 из 5  
**Приоритет:** Высокий  
**Оценка:** Тестирование и исправление багов  
**Лимит контекста:** 200k токенов

---

## Контекст задачи

Все основные функции реализованы. Необходимо провести комплексное тестирование, исправить найденные баги и убедиться, что всё работает корректно.

## Цель этапа

1. Провести полное тестирование всех функций
2. Исправить найденные баги
3. Проверить работу на разных устройствах
4. Убедиться в корректности данных в БД
5. Создать скрипт миграции для существующих Data URI (если нужно)

## Чек-лист тестирования

### Блок 1: Backend API

#### Тест 1.1: Публичный endpoint `/api/partners`

**Запрос:**
```bash
curl -X GET http://localhost:8000/api/partners
```

**Проверки:**
- ✅ Возвращает статус 200
- ✅ Возвращает массив партнёров
- ✅ Только активные партнёры (`is_active = true`)
- ✅ Сортировка по `display_order` ASC
- ✅ Поля маппятся правильно: `website_url`, `display_order`
- ✅ CORS заголовки присутствуют

#### Тест 1.2: Админский endpoint - GET

**Запрос:**
```bash
curl -X GET http://localhost:8000/api/c7b03587-cdba-48a4-ac48-9aa2775ff9a0 \
  -H "X-Admin-Token: YOUR_TOKEN"
```

**Проверки:**
- ✅ Требует авторизацию
- ✅ Возвращает все партнёры (активные и неактивные)
- ✅ Правильная структура данных

#### Тест 1.3: Админский endpoint - POST

**Запрос:**
```bash
curl -X POST http://localhost:8000/api/c7b03587-cdba-48a4-ac48-9aa2775ff9a0 \
  -H "X-Admin-Token: YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый партнёр",
    "logo_url": "/uploads/logos/test.png",
    "website_url": "https://example.com",
    "is_active": true
  }'
```

**Проверки:**
- ✅ Создаёт партнёра
- ✅ Автоматически назначает `sort_order`
- ✅ Возвращает созданный объект с ID
- ✅ Данные корректно сохраняются в БД

#### Тест 1.4: Админский endpoint - PUT

**Проверки:**
- ✅ Обновляет существующего партнёра
- ✅ Можно изменить `sort_order`
- ✅ Изменения сохраняются в БД

#### Тест 1.5: Админский endpoint - DELETE

**Проверки:**
- ✅ Удаляет партнёра из БД
- ✅ Возвращает успешный ответ
- ✅ Партнёр больше не появляется в списках

### Блок 2: Загрузка изображений

#### Тест 2.1: Upload endpoint

**Запрос:**
```bash
curl -X POST http://localhost:8000/api/6f0735b1-7477-4660-b2b0-0b694b4f36ea \
  -H "Content-Type: application/json" \
  -d '{
    "image": "BASE64_STRING",
    "filename": "test.png",
    "storage_type": "local",
    "folder": "logos"
  }'
```

**Проверки:**
- ✅ Файл сохраняется в `/public/uploads/logos/`
- ✅ Возвращает URL: `/uploads/logos/uuid.png`
- ✅ Файл доступен по этому URL

#### Тест 2.2: Валидация размера

**Проверки:**
- ✅ Файл > 2MB отклоняется
- ✅ Показывается ошибка "Размер файла не должен превышать 2 МБ"

#### Тест 2.3: Валидация типа

**Проверки:**
- ✅ Только изображения принимаются
- ✅ .txt, .pdf и другие отклоняются
- ✅ Показывается ошибка "Разрешены только изображения"

### Блок 3: Админ-панель

#### Тест 3.1: Создание партнёра

**Шаги:**
1. Открыть админ-панель → Контент → Логотипы партнёров
2. Нажать "Добавить партнёра"
3. Заполнить название: "Новый партнёр"
4. Заполнить сайт: "https://newpartner.com"
5. Загрузить логотип (PNG, 500KB)
6. Нажать "Сохранить"

**Проверки:**
- ✅ Форма открывается
- ✅ Поле "Порядок отображения" отсутствует
- ✅ Индикатор загрузки показывается
- ✅ Превью отображается
- ✅ Партнёр создаётся
- ✅ Появляется в списке
- ✅ Toast уведомление "Партнёр добавлен"

#### Тест 3.2: Редактирование партнёра

**Шаги:**
1. Нажать "Редактировать" на существующем партнёре
2. Изменить название
3. Изменить порядок отображения
4. Нажать "Сохранить"

**Проверки:**
- ✅ Режим редактирования активируется
- ✅ Поле "Порядок отображения" присутствует
- ✅ Изменения сохраняются
- ✅ Индикатор "Сохранение..." показывается
- ✅ Toast уведомление "Партнёр обновлён"

#### Тест 3.3: Замена логотипа

**Шаги:**
1. Навести на логотип в режиме просмотра
2. Нажать "Заменить"
3. Выбрать новый файл

**Проверки:**
- ✅ Кнопка "Заменить" появляется при наведении
- ✅ File dialog открывается
- ✅ Новый логотип загружается
- ✅ Автоматически сохраняется
- ✅ Превью обновляется

#### Тест 3.4: Удаление партнёра

**Шаги:**
1. Нажать "Удалить" на партнёре
2. Прочитать диалог подтверждения
3. Подтвердить удаление

**Проверки:**
- ✅ Диалог показывает название, сайт, статус
- ✅ Есть предупреждение "нельзя отменить"
- ✅ Партнёр удаляется из списка
- ✅ Toast уведомление с названием удалённого партнёра

#### Тест 3.5: Валидация в форме

**Проверки:**
- ✅ Пустое название → ошибка
- ✅ Название < 2 символов → ошибка
- ✅ Название > 100 символов → ошибка
- ✅ Пустой URL → ошибка
- ✅ URL без протокола → предупреждение
- ✅ Без логотипа → ошибка

### Блок 4: Главная страница

#### Тест 4.1: Отображение карусели

**Шаги:**
1. Создать 3+ активных партнёра
2. Открыть главную страницу
3. Прокрутить до карусели

**Проверки:**
- ✅ Карусель отображается
- ✅ Логотипы видны
- ✅ Анимация работает плавно
- ✅ Порядок соответствует `sort_order`
- ✅ Заголовок "Наши партнёры" отображается

#### Тест 4.2: Клик по логотипу

**Проверки:**
- ✅ Клик открывает сайт партнёра в новой вкладке
- ✅ URL корректный
- ✅ `rel="noopener noreferrer"` установлен

#### Тест 4.3: Фильтрация неактивных

**Шаги:**
1. Деактивировать одного партнёра в админке
2. Обновить главную страницу

**Проверки:**
- ✅ Неактивный партнёр НЕ отображается
- ✅ Активные партнёры остаются

#### Тест 4.4: Пустая карусель

**Шаги:**
1. Деактивировать всех партнёров
2. Обновить главную страницу

**Проверки:**
- ✅ Карусель не отображается (return null)
- ✅ Нет ошибок в консоли
- ✅ Страница выглядит нормально

#### Тест 4.5: Ошибки загрузки изображений

**Шаги:**
1. Создать партнёра с несуществующим logo_url
2. Открыть главную страницу

**Проверки:**
- ✅ Показывается текстовый fallback (название)
- ✅ Нет ошибок в консоли
- ✅ Карусель продолжает работать

### Блок 5: База данных

#### Тест 5.1: Структура данных

**SQL запрос:**
```sql
SELECT * FROM partners ORDER BY sort_order;
```

**Проверки:**
- ✅ Поле `logo_url` содержит путь `/uploads/logos/...`, а не Data URI
- ✅ Поле `sort_order` заполнено корректно
- ✅ Поле `is_active` имеет правильные значения
- ✅ Нет NULL в обязательных полях

#### Тест 5.2: Автоматический sort_order

**SQL запросы:**
```sql
-- Создать партнёра через API
-- Проверить sort_order
SELECT id, name, sort_order FROM partners ORDER BY id DESC LIMIT 1;
```

**Проверки:**
- ✅ `sort_order` = MAX(предыдущих) + 1
- ✅ Нет дублирования порядка

### Блок 6: Адаптивность

#### Тест 6.1: Мобильные устройства

**Проверки:**
- ✅ Админ-панель работает на мобильных
- ✅ Карусель адаптируется под ширину экрана
- ✅ Кнопки доступны и кликабельны
- ✅ Формы удобны для заполнения

#### Тест 6.2: Планшеты

**Проверки:**
- ✅ Интерфейс адаптирован
- ✅ Карусель отображается корректно

#### Тест 6.3: Desktop

**Проверки:**
- ✅ Полный функционал доступен
- ✅ Карусель занимает правильную ширину

### Блок 7: Производительность

#### Тест 7.1: Скорость загрузки

**Проверки:**
- ✅ Карусель загружается < 1 сек
- ✅ Логотипы кэшируются
- ✅ Нет лишних запросов к API

#### Тест 7.2: Размер изображений

**Проверки:**
- ✅ Логотипы оптимизированы
- ✅ Размер файлов разумный (< 200KB каждый)

## Скрипт миграции Data URI

Если в БД есть партнёры с Data URI, создай скрипт миграции:

**Файл:** `scripts/migrate-partner-logos.js`

```javascript
const fs = require('fs');
const path = require('path');
const { Pool } = require('pg');

async function migratePartnerLogos() {
  const pool = new Pool({ 
    connectionString: process.env.DATABASE_URL 
  });
  
  console.log('🔍 Поиск партнёров с Data URI...');
  
  const { rows } = await pool.query(
    "SELECT id, name, logo_url FROM partners WHERE logo_url LIKE 'data:%'"
  );
  
  if (rows.length === 0) {
    console.log('✅ Партнёров с Data URI не найдено');
    await pool.end();
    return;
  }
  
  console.log(`📦 Найдено ${rows.length} партнёров для миграции`);
  
  // Создать директорию если не существует
  const uploadDir = path.join(__dirname, '../public/uploads/logos');
  if (!fs.existsSync(uploadDir)) {
    fs.mkdirSync(uploadDir, { recursive: true });
    console.log('📁 Создана директория:', uploadDir);
  }
  
  for (const partner of rows) {
    try {
      // Извлечь base64 из Data URI
      const matches = partner.logo_url.match(/^data:([^;]+);base64,(.+)$/);
      if (!matches) {
        console.log(`⚠️  Пропущен ${partner.name}: некорректный Data URI`);
        continue;
      }
      
      const [, mimeType, base64Data] = matches;
      const ext = mimeType.split('/')[1] || 'png';
      const filename = `partner-${partner.id}-${Date.now()}.${ext}`;
      const filepath = path.join(uploadDir, filename);
      
      // Сохранить файл
      fs.writeFileSync(filepath, Buffer.from(base64Data, 'base64'));
      
      // Обновить БД
      await pool.query(
        'UPDATE partners SET logo_url = $1 WHERE id = $2',
        [`/uploads/logos/${filename}`, partner.id]
      );
      
      console.log(`✅ Мигрирован: ${partner.name} → ${filename}`);
    } catch (error) {
      console.error(`❌ Ошибка при миграции ${partner.name}:`, error.message);
    }
  }
  
  console.log('🎉 Миграция завершена!');
  await pool.end();
}

migratePartnerLogos().catch(console.error);
```

**Запуск:**
```bash
node scripts/migrate-partner-logos.js
```

## Исправление типичных багов

### Баг 1: CORS ошибки

**Симптомы:** Ошибка в консоли браузера

**Решение:** Проверить заголовки во всех endpoints:
```python
'Access-Control-Allow-Origin': '*',
'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS',
'Access-Control-Allow-Headers': 'Content-Type, X-Admin-Token'
```

### Баг 2: Изображения не загружаются

**Симптомы:** 404 на `/uploads/logos/...`

**Решение:**
1. Проверить что директория существует
2. Проверить права доступа (755)
3. Проверить nginx/apache конфигурацию для статики

### Баг 3: Карусель не анимируется

**Симптомы:** Логотипы статичны

**Решение:** Проверить CSS анимацию `animate-carousel` в tailwind.config

### Баг 4: Дублирование sort_order

**Симптомы:** Несколько партнёров с одинаковым порядком

**Решение:** Пересчитать порядок:
```sql
WITH numbered AS (
  SELECT id, ROW_NUMBER() OVER (ORDER BY sort_order, id) as new_order
  FROM partners
)
UPDATE partners p
SET sort_order = n.new_order
FROM numbered n
WHERE p.id = n.id;
```

## Финальный чек-лист

Перед завершением убедись:

- [ ] Все 5 этапов выполнены
- [ ] Все тесты пройдены
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах сервера
- [ ] Директория `/public/uploads/logos/` создана
- [ ] Права доступа настроены
- [ ] Миграция Data URI выполнена (если нужно)
- [ ] Документация обновлена
- [ ] Код прокомментирован
- [ ] Git коммиты сделаны

## Промпт для ИИ агента

```
Проведи финальное тестирование функционала логотипов партнёров.

ЗАДАЧА: Протестировать все функции и исправить баги

ТЕСТЫ:

1. Backend API:
   - GET /api/partners - публичный доступ, только активные
   - GET /api/admin-partner-logos - с авторизацией, все партнёры
   - POST - создание с автоматическим sort_order
   - PUT - обновление
   - DELETE - удаление

2. Загрузка изображений:
   - Файлы сохраняются в /public/uploads/logos/
   - Валидация размера (макс 2MB)
   - Валидация типа (только изображения)

3. Админ-панель:
   - Создание партнёра без поля "порядок"
   - Редактирование с полем "порядок"
   - Замена логотипа
   - Удаление с подтверждением
   - Валидация всех полей

4. Главная страница:
   - Карусель отображается
   - Анимация работает
   - Клик открывает сайт
   - Фильтрация неактивных
   - Fallback для ошибок

5. База данных:
   - logo_url содержит пути, не Data URI
   - sort_order корректный
   - Нет NULL в обязательных полях

ИСПРАВЬ найденные баги.

СОЗДАЙ скрипт миграции если есть Data URI в БД:
- scripts/migrate-partner-logos.js
- Конвертация Data URI в файлы
- Обновление БД

ПРОВЕРЬ:
- Нет ошибок в консоли
- Все функции работают
- Адаптивность на всех устройствах
- Производительность приемлемая

После тестирования создай отчёт о найденных и исправленных багах.
```
