# Восстановление функционала "Логотипы партнёров"

**Дата создания:** 2026-01-23  
**Версия:** 1.0  
**Статус:** Готово к реализации

## 📋 Обзор

Этот пакет документации содержит полный план восстановления функционала управления логотипами партнёров в админ-панели и их отображения на главной странице сайта.

## 🎯 Цели проекта

- ✅ Сохранение логотипов партнёров в базу данных
- ✅ Загрузка изображений в файловую систему (не Data URI)
- ✅ Отображение карусели логотипов на главной странице
- ✅ Полный CRUD функционал в админ-панели
- ✅ Автоматическая сортировка партнёров
- ✅ Публичный API для получения активных партнёров

## 📁 Структура документации

```
docs/partners-logos-2026-01-23/
├── README.md                    # Этот файл - инструкции по использованию
├── 00-analysis.md              # Анализ текущего состояния и проблем
├── 01-master-plan.md           # Детальный мастер-план реализации
├── 02-prompt-stage1.md         # Этап 1: Публичный API
├── 03-prompt-stage2.md         # Этап 2: Загрузка изображений
├── 04-prompt-stage3.md         # Этап 3: Автосортировка и карусель
├── 05-prompt-stage4.md         # Этап 4: UX улучшения
└── 06-prompt-stage5.md         # Этап 5: Тестирование
```

## 🚀 Быстрый старт

### Шаг 1: Ознакомление

Прочитай файлы в следующем порядке:

1. **README.md** (этот файл) - общая информация
2. **00-analysis.md** - понимание проблемы
3. **01-master-plan.md** - общий план действий

### Шаг 2: Подготовка окружения

Перед началом работы убедись:

```bash
# 1. База данных доступна
psql $DATABASE_URL -c "SELECT COUNT(*) FROM partners;"

# 2. Создай директорию для логотипов
mkdir -p public/uploads/logos
chmod 755 public/uploads/logos

# 3. Проверь что upload-image endpoint работает
curl -X OPTIONS http://localhost:8000/api/6f0735b1-7477-4660-b2b0-0b694b4f36ea
```

### Шаг 3: Поэтапная реализация

Выполняй этапы последовательно, используя промпты из соответствующих файлов.

## 📝 Этапы реализации

### Этап 1: Создание публичного API (02-prompt-stage1.md)

**Время:** ~30 минут  
**Сложность:** Низкая  
**Файлы:** 1 новый, 1 обновление

**Что делает:**
- Создаёт публичный endpoint `/api/partners`
- Возвращает только активные партнёры
- Не требует авторизации

**Как использовать:**
1. Открой файл `02-prompt-stage1.md`
2. Скопируй промпт из раздела "Промпт для ИИ агента"
3. Передай ИИ агенту (например, в Code режиме)
4. Проверь результат тестами из файла

**Критерий успеха:**
```bash
curl http://localhost:8000/api/partners
# Должен вернуть массив активных партнёров
```

---

### Этап 2: Интеграция загрузки изображений (03-prompt-stage2.md)

**Время:** ~1 час  
**Сложность:** Средняя  
**Файлы:** 1 новый хук, 3 обновления компонентов

**Что делает:**
- Создаёт хук `useImageUpload`
- Интегрирует загрузку файлов в формы
- Добавляет валидацию размера и типа
- Сохраняет файлы в `/public/uploads/logos/`

**Как использовать:**
1. Открой файл `03-prompt-stage2.md`
2. Скопируй промпт из раздела "Промпт для ИИ агента"
3. Передай ИИ агенту
4. Проверь что файлы сохраняются локально

**Критерий успеха:**
```bash
# После загрузки логотипа через админку
ls -la public/uploads/logos/
# Должны появиться файлы с UUID именами
```

---

### Этап 3: Автоматическая сортировка и карусель (04-prompt-stage3.md)

**Время:** ~45 минут  
**Сложность:** Низкая  
**Файлы:** 4 обновления

**Что делает:**
- Автоматически назначает `sort_order` при создании
- Убирает поле порядка из формы создания
- Добавляет карусель на главную страницу

**Как использовать:**
1. Открой файл `04-prompt-stage3.md`
2. Скопируй промпт из раздела "Промпт для ИИ агента"
3. Передай ИИ агенту
4. Проверь карусель на главной странице

**Критерий успеха:**
- Создание партнёра без указания порядка работает
- Карусель видна на главной странице
- Логотипы анимируются

---

### Этап 4: Улучшения UX (05-prompt-stage4.md)

**Время:** ~1 час  
**Сложность:** Средняя  
**Файлы:** 4 обновления

**Что делает:**
- Добавляет валидацию URL и названия
- Улучшает подтверждение удаления
- Добавляет возможность замены логотипа
- Добавляет индикаторы состояния

**Как использовать:**
1. Открой файл `05-prompt-stage4.md`
2. Скопируй промпт из раздела "Промпт для ИИ агента"
3. Передай ИИ агенту
4. Протестируй все улучшения

**Критерий успеха:**
- Валидация работает для всех полей
- Можно заменить логотип без полного редактирования
- Индикаторы показывают состояние операций

---

### Этап 5: Финальное тестирование (06-prompt-stage5.md)

**Время:** ~1-2 часа  
**Сложность:** Средняя  
**Файлы:** Тестирование всех компонентов

**Что делает:**
- Комплексное тестирование всех функций
- Исправление найденных багов
- Миграция существующих Data URI (если нужно)
- Проверка на разных устройствах

**Как использовать:**
1. Открой файл `06-prompt-stage5.md`
2. Пройди по всем тестам из чек-листа
3. Используй промпт для исправления багов
4. Запусти скрипт миграции если нужно

**Критерий успеха:**
- Все тесты пройдены
- Нет ошибок в консоли
- Функционал работает на всех устройствах

## 🔧 Работа с ИИ агентом

### Рекомендации по использованию промптов

1. **Последовательность:** Выполняй этапы по порядку, не пропуская
2. **Проверка:** После каждого этапа проверяй результат
3. **Контекст:** Если агент теряет контекст, напомни о предыдущих этапах
4. **Лимит токенов:** Каждый промпт рассчитан на лимит 200k токенов

### Формат работы с промптами

Каждый файл этапа содержит:

1. **Контекст задачи** - что уже сделано, что нужно сделать
2. **Технические требования** - детальные спецификации
3. **Примеры кода** - готовые решения для копирования
4. **Критерии приёмки** - как проверить что всё работает
5. **Тестирование** - конкретные тесты для проверки
6. **Промпт для ИИ агента** - краткая версия для копирования

### Пример использования

```
1. Открой 02-prompt-stage1.md
2. Прочитай раздел "Контекст задачи"
3. Скопируй промпт из раздела "Промпт для ИИ агента"
4. Переключись в Code режим
5. Вставь промпт
6. Дождись выполнения
7. Проверь результат тестами из раздела "Тестирование"
8. Если всё работает - переходи к следующему этапу
```

## 📊 Прогресс выполнения

Отмечай выполненные этапы:

- [ ] Этап 1: Публичный API
- [ ] Этап 2: Загрузка изображений
- [ ] Этап 3: Автосортировка и карусель
- [ ] Этап 4: Улучшения UX
- [ ] Этап 5: Финальное тестирование

## 🐛 Решение проблем

### Проблема: Директория не создаётся

```bash
mkdir -p public/uploads/logos
chmod 755 public/uploads/logos
```

### Проблема: CORS ошибки

Проверь заголовки в backend endpoints:
```python
'Access-Control-Allow-Origin': '*'
```

### Проблема: Изображения не загружаются

1. Проверь права доступа к директории
2. Проверь что nginx/apache отдаёт статику
3. Проверь путь в БД (должен быть `/uploads/logos/...`)

### Проблема: Карусель не отображается

1. Проверь что endpoint `/api/partners` работает
2. Проверь что есть активные партнёры в БД
3. Проверь консоль браузера на ошибки
4. Проверь что `PartnersCarousel` импортирован в `Index.tsx`

## 📈 Метрики успеха

После завершения всех этапов должно быть:

### Функциональность
- ✅ 100% CRUD операций работают
- ✅ Карусель отображается на главной
- ✅ Логотипы загружаются корректно

### Производительность
- ✅ Время загрузки карусели < 1 сек
- ✅ Размер БД не увеличивается от изображений
- ✅ Плавная анимация (60 FPS)

### UX
- ✅ Все действия имеют обратную связь
- ✅ Понятные сообщения об ошибках
- ✅ Интуитивный интерфейс

### Надёжность
- ✅ Обработка всех ошибок
- ✅ Валидация данных
- ✅ Нет потери данных

## 🔍 Проверка результата

После завершения всех этапов выполни:

```bash
# 1. Проверь структуру БД
psql $DATABASE_URL -c "SELECT id, name, logo_url, sort_order, is_active FROM partners ORDER BY sort_order;"

# 2. Проверь файлы
ls -la public/uploads/logos/

# 3. Проверь публичный API
curl http://localhost:8000/api/partners | jq

# 4. Проверь админский API
curl -H "X-Admin-Token: YOUR_TOKEN" http://localhost:8000/api/c7b03587-cdba-48a4-ac48-9aa2775ff9a0 | jq
```

## 📚 Дополнительные материалы

### Связанные документы

- `backend/admin-partner-logos/index.py` - Админский API
- `backend/upload-image/index.py` - API загрузки изображений
- `src/components/PartnersCarousel.tsx` - Компонент карусели
- `src/components/admin/PartnersAdmin.tsx` - Админ-панель

### Полезные команды

```bash
# Просмотр логов backend
tail -f logs/backend.log

# Проверка размера БД
psql $DATABASE_URL -c "SELECT pg_size_pretty(pg_total_relation_size('partners'));"

# Очистка тестовых данных
psql $DATABASE_URL -c "DELETE FROM partners WHERE name LIKE 'Тест%';"

# Пересчёт sort_order
psql $DATABASE_URL -c "
WITH numbered AS (
  SELECT id, ROW_NUMBER() OVER (ORDER BY sort_order, id) as new_order
  FROM partners
)
UPDATE partners p SET sort_order = n.new_order FROM numbered n WHERE p.id = n.id;
"
```

## 🎓 Обучение

Если ты новичок в проекте, рекомендуется:

1. Прочитать `00-analysis.md` для понимания архитектуры
2. Изучить `01-master-plan.md` для общего видения
3. Посмотреть существующий код компонентов
4. Начать с Этапа 1 и двигаться последовательно

## 💡 Советы

1. **Делай коммиты после каждого этапа** - так проще откатиться при ошибке
2. **Тестируй на каждом этапе** - не накапливай баги
3. **Сохраняй скриншоты** - для документации и отчётов
4. **Делай бэкапы БД** - перед миграцией данных

## 🤝 Поддержка

Если возникли вопросы:

1. Проверь раздел "Решение проблем" в этом файле
2. Посмотри раздел "Возможные проблемы" в соответствующем этапе
3. Проверь консоль браузера и логи сервера
4. Обратись к документации проекта

## 📝 Changelog

### Версия 1.0 (2026-01-23)
- Создана полная документация
- 5 этапов реализации
- Промпты для ИИ агента
- Тесты и чек-листы

## 📄 Лицензия

Эта документация является частью проекта и следует той же лицензии.

---

**Удачи в восстановлении функционала! 🚀**

Если всё сделано правильно, у тебя будет полностью рабочий функционал управления логотипами партнёров с красивой каруселью на главной странице.
