# Pixel - Self-Hosted Version

Это версия проекта Pixel для самостоятельного хостинга на вашем сервере.

## 🎯 Что изменилось?

### Было (на poehali.dev):
- ✅ 30 cloud functions на платформе
- ✅ PostgreSQL база данных (управляемая)
- ✅ S3 хранилище на cdn.poehali.dev
- ✅ Автоматический деплой

### Стало (self-hosted):
- ✅ Express.js API сервер (все функции в одном месте)
- ✅ PostgreSQL в Docker контейнере
- ✅ MinIO S3-совместимое хранилище
- ✅ Nginx для frontend и reverse proxy
- ✅ Автоматические SSL сертификаты (Let's Encrypt)
- ✅ Docker Compose для легкого деплоя

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone <your-repo-url>
cd pixel-project
```

### 2. Настройте окружение

```bash
cp server/.env.example server/.env
nano server/.env  # Укажите пароли и ключи
```

### 3. Запустите проект

```bash
# На сервере с Docker
docker-compose up -d

# Проверьте статус
docker-compose ps
```

### 4. Импортируйте данные

```bash
# Импортируйте базу данных
./scripts/import-database.sh your_database_dump.sql

# Настройте SSL
./scripts/setup-ssl.sh pixel59.ru
```

## 📁 Структура проекта

```
pixel-project/
├── server/                 # Backend API (Express.js)
│   ├── api/               # API endpoints
│   ├── config/            # Конфигурация (DB, S3)
│   ├── middleware/        # Middleware (auth, cors)
│   ├── server.js          # Главный файл сервера
│   └── .env               # Переменные окружения
├── src/                   # Frontend (React + Vite)
├── scripts/               # Скрипты для деплоя
├── docker-compose.yml     # Docker конфигурация
├── nginx.conf             # Nginx конфигурация
├── DEPLOY.md             # Полное руководство по деплою
└── MIGRATION_GUIDE.md    # Руководство по миграции
```

## 📚 Документация

- **[DEPLOY.md](DEPLOY.md)** - Пошаговое руководство по развёртыванию
- **[MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)** - Миграция с poehali.dev
- **[server/README.md](server/README.md)** - Документация API

## 🔧 Основные команды

### Управление сервисами

```bash
# Запустить все сервисы
docker-compose up -d

# Остановить все сервисы
docker-compose down

# Перезапустить сервисы
docker-compose restart

# Просмотр логов
docker-compose logs -f api

# Обновить сервис после изменений
docker-compose up -d --build api
```

### Разработка

```bash
# Frontend (локально)
npm install
npm run dev

# Backend (локально)
cd server
npm install
npm run dev
```

### Обслуживание

```bash
# Бэкап базы данных
docker-compose exec postgres pg_dump -U pixel_user pixel_db > backup.sql

# Очистка Docker
docker system prune -a

# Просмотр ресурсов
docker stats
```

## 🌐 API Endpoints

После запуска API доступен по адресу: `https://api.pixel59.ru/api`

### Публичные endpoints:
- `POST /api/contact` - Отправка контактной формы
- `GET /api/portfolio` - Список проектов портфолио
- `GET /api/partners/logos` - Логотипы партнеров

### Защищенные endpoints (требуют токен):
- `POST /api/portfolio` - Создание проекта
- `PUT /api/portfolio/:id` - Обновление проекта
- `DELETE /api/portfolio/:id` - Удаление проекта

Полная документация API: [server/API.md](server/API.md)

## 🔐 Безопасность

### Важные переменные окружения:

```env
ADMIN_PASSWORD_HASH    # Хеш пароля администратора (bcrypt)
JWT_SECRET             # Секрет для JWT токенов
DB_PASSWORD            # Пароль базы данных
S3_ACCESS_KEY          # Ключ доступа MinIO
S3_SECRET_KEY          # Секретный ключ MinIO
```

**Никогда не коммитьте файл `.env` в Git!**

## 📊 Мониторинг

### Проверка здоровья сервисов:

```bash
# API Health Check
curl https://api.pixel59.ru/health

# PostgreSQL
docker-compose exec postgres pg_isready

# MinIO
curl http://localhost:9000/minio/health/live
```

### Логи:

```bash
# Все логи
docker-compose logs

# Конкретный сервис
docker-compose logs -f api
docker-compose logs -f nginx
docker-compose logs -f postgres
```

## 🐛 Решение проблем

### API не запускается

```bash
# Проверьте логи
docker-compose logs api

# Проверьте что БД запущена
docker-compose ps postgres

# Проверьте .env файл
cat server/.env
```

### База данных не подключается

```bash
# Проверьте пароль в .env
# Перезапустите PostgreSQL
docker-compose restart postgres
```

### Frontend не загружается

```bash
# Проверьте что билд скопирован в dist/
ls -la dist/

# Проверьте логи Nginx
docker-compose logs nginx
```

## 💰 Стоимость

**Ежемесячные расходы:**
- VPS (2GB RAM, 20GB SSD): ~700₽/мес
- Домен .ru: ~200₽/год
- SSL: Бесплатно (Let's Encrypt)

**Итого: ~700₽/месяц**

## 🤝 Поддержка

Если возникли вопросы:
1. Проверьте [DEPLOY.md](DEPLOY.md)
2. Посмотрите логи: `docker-compose logs`
3. Создайте issue в репозитории

## 📝 Лицензия

MIT License - свободное использование

---

**Успешного деплоя! 🚀**
