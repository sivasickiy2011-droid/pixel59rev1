# 🚀 Полное руководство по развёртыванию на собственном сервере

## Подготовка

### 1. Требования к серверу
- **VPS**: минимум 2GB RAM, 20GB SSD, 2 CPU
- **OS**: Ubuntu 22.04 LTS
- **Доступ**: SSH с правами root
- **Домен**: pixel59.ru с настроенными DNS записями

### 2. Настройка DNS

Добавьте следующие A-записи на ваш домен:

```
pixel59.ru          A    IP_ВАШЕГО_СЕРВЕРА
www.pixel59.ru      A    IP_ВАШЕГО_СЕРВЕРА
api.pixel59.ru      A    IP_ВАШЕГО_СЕРВЕРА
cdn.pixel59.ru      A    IP_ВАШЕГО_СЕРВЕРА
```

Проверьте: `dig pixel59.ru +short` (должен показать ваш IP)

---

## Установка на сервер

### Шаг 1: Подключение к серверу

```bash
ssh root@IP_ВАШЕГО_СЕРВЕРА
```

### Шаг 2: Автоматическая настройка

```bash
# Скачайте и запустите скрипт настройки
wget https://raw.githubusercontent.com/YOUR_REPO/main/scripts/setup-server.sh
chmod +x setup-server.sh
sudo ./setup-server.sh
```

Скрипт установит:
- Docker & Docker Compose
- UFW (firewall)
- Необходимые пакеты

### Шаг 3: Клонирование проекта

```bash
# Если репозиторий приватный, настройте SSH ключ
git clone git@github.com:YOUR_USERNAME/pixel-project.git
cd pixel-project
```

### Шаг 4: Конфигурация окружения

```bash
# Скопируйте пример конфигурации
cp server/.env.example server/.env

# Отредактируйте конфигурацию
nano server/.env
```

**Важные переменные для настройки:**

```env
# Database
DB_PASSWORD=ваш_сильный_пароль_для_postgres

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET=ваш_jwt_secret_32_символа_минимум

# Admin Password Hash (сгенерируйте через bcrypt)
# Используйте: node -e "console.log(require('bcrypt').hashSync('ваш_пароль', 10))"
ADMIN_PASSWORD_HASH=$2b$10$ваш_bcrypt_хеш

# S3/MinIO
S3_ACCESS_KEY=admin_ключ_для_minio
S3_SECRET_KEY=секретный_ключ_для_minio_минимум_32_символа

# Yandex API
YANDEX_METRIKA_TOKEN=ваш_токен_яндекс_метрики
YANDEX_WEBMASTER_TOKEN=ваш_токен_вебмастера
YANDEX_METRIKA_COUNTER_ID=101026698

# Telegram (если используете)
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id

# Bitrix24 (если используете)
BITRIX24_WEBHOOK_URL=https://ваш_портал.bitrix24.ru/rest/1/ваш_webhook/

# CORS (домены через запятую)
CORS_ORIGINS=https://pixel59.ru,https://www.pixel59.ru
```

### Шаг 5: Экспорт базы данных из poehali.dev

**На вашем локальном компьютере:**

```bash
# Получите параметры подключения к БД из poehali.dev
# (они в секретах проекта как DATABASE_URL)

# Экспортируйте базу данных
PGPASSWORD=пароль pg_dump \
  -h хост \
  -p 5432 \
  -U пользователь \
  -d база_данных \
  -F p \
  --clean \
  -f pixel_db_export.sql

# Загрузите дамп на сервер
scp pixel_db_export.sql root@IP_СЕРВЕРА:/root/pixel-project/
```

### Шаг 6: Запуск проекта

```bash
# Запустите все сервисы через Docker Compose
docker-compose up -d

# Проверьте статус
docker-compose ps
```

Должны быть запущены:
- `pixel-db` (PostgreSQL)
- `pixel-minio` (S3 хранилище)
- `pixel-api` (Backend API)
- `pixel-nginx` (Web сервер)
- `pixel-certbot` (SSL сертификаты)

### Шаг 7: Импорт базы данных

```bash
# Импортируйте дамп в PostgreSQL
chmod +x scripts/import-database.sh
./scripts/import-database.sh pixel_db_export.sql
```

### Шаг 8: Настройка SSL

```bash
chmod +x scripts/setup-ssl.sh
./scripts/setup-ssl.sh pixel59.ru
```

Скрипт автоматически:
- Получит SSL сертификаты от Let's Encrypt
- Настроит HTTPS для всех поддоменов
- Перезапустит Nginx

### Шаг 9: Сборка и загрузка Frontend

**На локальном компьютере:**

```bash
# Установите зависимости
npm install

# Соберите production билд
npm run build

# Загрузите на сервер
scp -r dist/* root@IP_СЕРВЕРА:/root/pixel-project/dist/
```

### Шаг 10: Настройка MinIO (S3 хранилище)

```bash
# Откройте MinIO консоль в браузере
# http://IP_СЕРВЕРА:9001

# Логин: значение S3_ACCESS_KEY из .env
# Пароль: значение S3_SECRET_KEY из .env

# Создайте bucket с именем: pixel-files
# Установите политику доступа: public (для публичных файлов)
```

### Шаг 11: Миграция файлов из poehali.dev CDN

```bash
# Скачайте все файлы с cdn.poehali.dev
# и загрузите их в MinIO bucket pixel-files
```

---

## Проверка работы

### 1. Проверьте здоровье API:

```bash
curl https://api.pixel59.ru/health
# Ответ: {"status":"ok","timestamp":"..."}
```

### 2. Проверьте frontend:

Откройте в браузере: `https://pixel59.ru`

### 3. Проверьте логи:

```bash
# Логи API
docker-compose logs -f api

# Логи Nginx
docker-compose logs -f nginx

# Логи базы данных
docker-compose logs -f postgres
```

---

## Обслуживание

### Перезапуск сервисов

```bash
# Перезапустить все сервисы
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart api
```

### Обновление проекта

```bash
# Получить последние изменения из Git
git pull origin main

# Пересобрать и перезапустить API
docker-compose up -d --build api

# Обновить frontend (на локальной машине)
npm run build
scp -r dist/* root@IP_СЕРВЕРА:/root/pixel-project/dist/
```

### Бэкап базы данных

```bash
# Автоматический бэкап
docker-compose exec postgres pg_dump -U pixel_user pixel_db > backup_$(date +%Y%m%d).sql

# Настройте cron для ежедневного бэкапа
crontab -e
# Добавьте: 0 3 * * * cd /root/pixel-project && docker-compose exec postgres pg_dump -U pixel_user pixel_db > /backups/backup_$(date +\%Y\%m\%d).sql
```

### Просмотр логов

```bash
# Все логи
docker-compose logs

# Логи API в реальном времени
docker-compose logs -f api

# Последние 100 строк логов
docker-compose logs --tail=100 api
```

### Мониторинг ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Место на диске
df -h

# Память
free -h
```

---

## Решение проблем

### API не отвечает

```bash
# Проверьте статус
docker-compose ps

# Перезапустите API
docker-compose restart api

# Проверьте логи
docker-compose logs api
```

### База данных не подключается

```bash
# Проверьте что PostgreSQL запущен
docker-compose ps postgres

# Проверьте подключение
docker-compose exec postgres psql -U pixel_user -d pixel_db -c "SELECT 1"
```

### SSL не работает

```bash
# Перезапустите certbot
./scripts/setup-ssl.sh pixel59.ru

# Проверьте сертификаты
docker-compose exec nginx ls -la /etc/letsencrypt/live/pixel59.ru/
```

---

## Безопасность

### 1. Смена паролей

```bash
# Изменить пароль БД
docker-compose exec postgres psql -U postgres
ALTER USER pixel_user WITH PASSWORD 'новый_пароль';

# Не забудьте обновить в .env и перезапустить API
```

### 2. Обновление системы

```bash
# Регулярно обновляйте сервер
sudo apt update && sudo apt upgrade -y
```

### 3. Firewall

```bash
# Проверьте что открыты только нужные порты
sudo ufw status

# Должны быть открыты:
# 22/tcp (SSH)
# 80/tcp (HTTP)
# 443/tcp (HTTPS)
```

---

## Производительность

### Оптимизация PostgreSQL

В `docker-compose.yml` добавьте команду с параметрами:

```yaml
postgres:
  command: postgres -c shared_buffers=256MB -c max_connections=200
```

### Кеширование в Nginx

Уже настроено в `nginx.conf`:
- Статические файлы кешируются на 1 год
- Gzip сжатие включено

### Масштабирование API

Для увеличения нагрузки запустите несколько инстансов API:

```yaml
api:
  deploy:
    replicas: 3  # Запустить 3 копии API
```

---

## Стоимость инфраструктуры

**Примерные цены (в месяц):**

- VPS 2GB RAM: **500-1000₽** (reg.ru, timeweb.ru)
- Домен .ru: **200₽/год**
- SSL сертификаты: **Бесплатно** (Let's Encrypt)
- Хранилище MinIO: **0₽** (на вашем VPS)

**Итого: ~700₽/месяц** за полную независимость

---

## Контакты

Если возникли вопросы по развёртыванию:
1. Проверьте логи: `docker-compose logs`
2. Проверьте документацию в `MIGRATION_GUIDE.md`
3. Напишите в поддержку проекта

Удачи! 🚀
