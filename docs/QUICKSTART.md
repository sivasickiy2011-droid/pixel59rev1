# 🚀 Быстрый старт для деплоя на свой сервер

## За 10 минут до запуска

### 1️⃣ Подготовка (2 мин)

```bash
# На сервере Ubuntu 22.04
ssh root@ваш_сервер

# Автоустановка всего необходимого
wget https://raw.githubusercontent.com/YOUR_REPO/main/scripts/setup-server.sh
chmod +x setup-server.sh
./setup-server.sh
```

### 2️⃣ Настройка проекта (3 мин)

```bash
# Клонируйте проект
git clone <repo-url>
cd pixel-project

# Создайте .env из примера
cp server/.env.example server/.env
nano server/.env
```

**Минимальные обязательные параметры:**

```env
DB_PASSWORD=сложный_пароль_123
JWT_SECRET=случайная_строка_32_символа
ADMIN_PASSWORD_HASH=$2b$10$ваш_bcrypt_хеш
S3_ACCESS_KEY=admin
S3_SECRET_KEY=сложный_пароль_для_minio_32+
```

**Генерация bcrypt хеша:**
```bash
node -e "console.log(require('bcrypt').hashSync('ваш_пароль', 10))"
```

### 3️⃣ Запуск (2 мин)

```bash
# Запустите все сервисы
docker-compose up -d

# Проверьте что всё работает
docker-compose ps
```

### 4️⃣ Импорт данных (2 мин)

```bash
# Импортируйте БД
./scripts/import-database.sh ваш_дамп.sql

# Настройте SSL
./scripts/setup-ssl.sh pixel59.ru
```

### 5️⃣ Деплой frontend (1 мин)

```bash
# На локальной машине
npm run build

# Загрузите на сервер
scp -r dist/* root@IP:/root/pixel-project/dist/
```

## ✅ Готово!

Откройте в браузере: `https://pixel59.ru`

---

## 🔧 Что делать дальше?

### Миграция файлов (изображения, документы)

```bash
# В MinIO консоли (http://IP:9001)
# Логин/пароль: S3_ACCESS_KEY / S3_SECRET_KEY

1. Создайте bucket: pixel-files
2. Установите публичный доступ
3. Загрузите файлы из cdn.poehali.dev
```

### Настройка автообновления SSL

```bash
# SSL обновляется автоматически каждые 12 часов
# Проверить можно через:
docker-compose logs certbot
```

### Мониторинг

```bash
# Логи API
docker-compose logs -f api

# Статус сервисов
docker-compose ps

# Использование ресурсов
docker stats
```

---

## 🆘 Проблемы?

### API не запускается

```bash
docker-compose logs api
# Проверьте что DB_PASSWORD в .env совпадает с паролем БД
```

### База данных не подключается

```bash
docker-compose restart postgres
docker-compose logs postgres
```

### SSL не работает

```bash
# Проверьте DNS записи
dig pixel59.ru +short

# Попробуйте снова
./scripts/setup-ssl.sh pixel59.ru
```

---

## 📚 Полная документация

- **Подробный деплой**: [DEPLOY.md](DEPLOY.md)
- **Миграция с poehali**: [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)
- **API документация**: [server/README.md](server/README.md)

---

## 💰 Стоимость

- VPS 2GB: ~700₽/мес
- Домен .ru: ~200₽/год
- **Итого: ~700₽/мес**

Полная независимость от платформ! 🎉
