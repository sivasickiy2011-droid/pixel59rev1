# Этап 4.5: Медиа и визуализация карточек новостей

**Файлы для изменения:** `backend/news-admin/index.py`, `backend/news-feed/index.py`, `src/components/News.tsx`
**Приоритет:** Средний
**Сложность:** Средняя
**Примерный объем:** ~140 строк кода + стили

---

## 🎯 Цель этапа

Покрыть медиавизуализацию карточек новостей: каждая карточка должна показывать основную картинку (или заглушку) и подставлять iframe для YouTube/Vimeo, если в тексте есть поддерживаемая ссылка. Не нарушить ограничения мастер-плана (минимум 500 символов контента, перевод в пределах 5000 символов, чистый текст).

---

## 📋 Задачи

1. ✅ Определить главный визуальный ресурс: добывать изображение из RSS (media_content, enclosures) или из веб-страницы (`og:image`, `twitter:image`, первый `<img>` в `readability`), и сохранять его в поле `image_url`. Если изображение не найдено, подставлять заглушку `dist-preview/placeholder.svg`.
2. ✅ Найти ссылки на YouTube и Vimeo в контенте (`entry.links`, `entry.summary`, `fetch_article_content`), сформировать безопасный `video_embed_url` (`https://www.youtube.com/embed/...` или `https://player.vimeo.com/video/...`) и передавать его в front-end.
3. ✅ Обновить API вывода (`backend/news-feed/index.py`) и фронтенд (`src/components/News.tsx`) так, чтобы карточка отображала изображение с атрибутом `loading="lazy"`, использовала заглушку при ошибке загрузки и вставляла `<iframe>` с `allowFullScreen`/`loading="lazy"`, когда `video_embed_url` присутствует.
4. ✅ Поддерживать существующую логику перевода и очистки: никаких изменений в структуре БД, не трогать `original_content`/`translated_content`, но использовать уже очищенный текст для поиска видео.
5. ✅ Добавить тестовые проверки (ручные) на fallback к заглушке и на вставку iframe при наличии видео.

---

## 🔧 Промпт для ИИ агента

```
КОНТЕКСТ:
Этапы 1–4 завершены: текст извлекается, переводится, очищается и сохраняется. Сейчас нужно сделать так, чтобы карточки новостей выглядели завершенными: показывали изображение и включали видео, когда оно есть. Не добавляй новых полей в БД, но используй уже существующее поле `image_url`, проверяй контент после функцией `clean_html()` и ограничения 5000 символов для перевода.

ЗАДАНИЕ:

1. Добавь в `backend/news-admin/index.py` (около секции, где формируется словарь `news_item`) логики для `image_url` и `video_embed_url`:

```python
def extract_featured_image(entry, article_html: str = '') -> str:
    """Ищет первое подходящее изображение. Отдает заглушку, если ничего не найдено."""
    candidates = []
    if hasattr(entry, 'media_content'):
        candidates.extend(item.get('url') for item in entry.media_content if isinstance(item, dict))
    if hasattr(entry, 'enclosures'):
        candidates.extend(item.get('href') for item in entry.enclosures if isinstance(item, dict))
    og_match = re.search(r'<meta property="og:image" content="([^"]+)"', article_html)
    if og_match:
        candidates.append(og_match.group(1))
    twitter_match = re.search(r'<meta name="twitter:image" content="([^"]+)"', article_html)
    if twitter_match:
        candidates.append(twitter_match.group(1))
    for url in candidates:
        if url:
            return normalize_image_url(url)
    return '/dist-preview/placeholder.svg' if not candidates else '/dist-preview/placeholder.svg'

def extract_video_embed(entry, cleaned_text: str, article_html: str = '') -> str:
    """Ищет YouTube/Vimeo ссылки и возвращает embed-формат."""
    text_sources = [cleaned_text, article_html, getattr(entry, 'summary', ''), getattr(entry, 'description', '')]
    match = None
    for source in text_sources:
        if not source:
            continue
        yt = re.search(r'(?:https?://)?(?:www\.)?(?:youtube\.com/watch\?v=|youtu\.be/)([A-Za-z0-9_-]+)', source)
        if yt:
            match = f"https://www.youtube.com/embed/{yt.group(1)}"
            break
        vimeo = re.search(r'(?:https?://)?(?:www\.)?player\.vimeo\.com/video/([0-9]+)', source)
        if vimeo:
            match = f"https://player.vimeo.com/video/{vimeo.group(1)}"
            break
    return match or ''
```

2. При формировании `news_item` сохраняй `image_url` и `video_embed_url`, но не меняй способ сохранения `original_content` и `translated_content`.

3. В `backend/news-feed/index.py` расширь модель ответа, чтобы `image_url` и `video_embed_url` передавались фронтенду, и чтобы `image_url` никогда не был пустым (использовать заглушку).

4. В `src/components/News.tsx`:
   - Показывай `<img src={image_url} alt={translated_title} loading="lazy" onError={(event) => (event.currentTarget.src = '/dist-preview/placeholder.svg')}/>` в верхней части карточки.
   - Если `video_embed_url` есть, подставляй `<iframe src={video_embed_url} title={translated_title} loading="lazy" referrerPolicy="no-referrer" allowFullScreen></iframe>` под заголовком.
   - Оборачивай iframe в контейнер с фиксированной пропорцией (например, 16:9).

5. Добавь сопроводительные комментарии на русском, чтобы было понятно, что placeholder используется при отсутствии изображения и что iframe вставляется только для YouTube/Vimeo.

6. Документируй в README секции тестирования:
   - Проверь карточку новости без изображения (например, искусственно очистить `image_url`), убедись, что подставляется `dist-preview/placeholder.svg`.
   - Проверь карточку с YouTube-ссылкой в тексте — iframe должен появиться и быть кликабельным.

ТРЕБОВАНИЯ:
 - Не добавляй новые столбцы в таблицу `news`.
 - Не меняй уже работающую очистку/перевод.
 - Пользовательское видео смотрится только через YouTube или Vimeo.
 - Все новые права доступа работают без отключений или новой инфраструктуры.

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
Новость имеет изображение (или заглушку) и встроенное видео, если ссылка есть, без нарушения текущих ограничений на контент и перевод.

ТЕСТИРОВАНИЕ:
1. Обновить ленту новостей через API, удостовериться, что в БД появились `image_url` и (при наличии видео) `video_embed_url`.
2. Добавить тестовую новость с только YouTube-ссылкой и убедиться, что фронтенд показывает `<iframe>`.
3. Удалить `image_url` из ответа API и убедиться, что фронтенд отображает `dist-preview/placeholder.svg`.
```

---

## ✅ Критерии успеха

1. Поле `image_url` всегда заполнено (работает placeholder).
2. `video_embed_url` содержит только YouTube/Vimeo embed.
3. Карточка показывает изображение и iframe без разрыва текста/перевода.

---

## 🧪 Проверки на фронтенде

1. Открой карточку новости без картинки и проверь `img` с `src='/dist-preview/placeholder.svg'`.
2. Открой новость с видео и убедись, что iframe выглядит нормально на разных ширинах.

---

**Примечание:** Этап 4.5 дополняет этап 4 и предшествует финальному этапу 5, не нарушая общей последовательности.
