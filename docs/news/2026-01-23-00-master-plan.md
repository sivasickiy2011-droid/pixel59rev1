# Мастер-план: Исправление функционала новостей в Админ Панели

**Дата:** 2026-01-23  
**Раздел:** Админ Панель → Контент → Новости  
**Проблема:** Отсутствие оригинального и переведенного контента новостей

---

## 🔍 Анализ проблемы

### Текущее состояние
По результатам анализа базы данных и кода выявлено:

1. **База данных:**
   - Поля `original_content` и `translated_content` пустые (LENGTH = 0)
   - Заголовки (`original_title`, `translated_title`) заполняются корректно
   - Выдержки (`original_excerpt`, `translated_excerpt`) заполняются корректно
   - Ссылки (`link`) сохраняются правильно

2. **Код backend/news-admin/index.py:**
   - RSS-ленты парсятся через `feedparser`
   - Функция `clean_html()` очищает HTML-теги из контента
   - Функция `translate_text()` использует Ollama (llama3.2) для перевода
   - Функция `translate_full_content()` ограничивает контент до 2000 символов
   - **ПРОБЛЕМА:** Контент извлекается из `entry.summary`, но многие RSS-ленты не содержат полного текста в summary

### Корневые причины

1. **Недостаточный контент в RSS:**
   - RSS-ленты (Hacker News, Netlify) предоставляют только краткое описание в `entry.summary`
   - Полный текст статьи находится на внешнем сайте по ссылке `entry.link`
   - Текущий код не извлекает контент со страницы статьи

2. **Логика извлечения контента:**
   ```python
   # Строки 243-251 в backend/news-admin/index.py
   original_content_raw = entry.summary if hasattr(entry, 'summary') else ''
   original_content = clean_html(original_content_raw)
   ```
   - Используется только `entry.summary`, который часто пустой или очень короткий
   - Нет попытки получить полный текст из `entry.content` или со страницы

3. **Ограничения Ollama:**
   - Перевод ограничен 2000 символами
   - Температура 0.1 может давать слишком буквальный перевод
   - Промпт может быть недостаточно точным

---

## 🎯 Цели исправления

1. ✅ Извлекать полный контент статей из RSS-лент
2. ✅ Получать текст со страниц статей при необходимости (web scraping)
3. ✅ Улучшить качество перевода через Ollama
4. ✅ Оптимизировать промпты для модели llama3.2
5. ✅ Обеспечить корректную очистку HTML и метаданных
6. ✅ Сохранять контент в БД с правильной сортировкой (новые впереди)

---

## 📋 План исправления

### Этап 1: Улучшение извлечения контента из RSS
**Файл:** `backend/news-admin/index.py`

**Задачи:**
- Проверять `entry.content` в дополнение к `entry.summary`
- Использовать `entry.description` как fallback
- Объединять все доступные текстовые поля
- Увеличить лимит извлекаемого текста

### Этап 2: Добавление web scraping для полного контента
**Новый функционал:**
- Создать функцию `fetch_article_content(url)` для извлечения текста со страницы
- Использовать библиотеки: `requests`, `beautifulsoup4`, `readability-lxml`
- Извлекать основной контент статьи, игнорируя навигацию и рекламу
- Добавить обработку ошибок и таймауты

### Этап 3: Оптимизация перевода через Ollama
**Улучшения:**
- Переработать промпт для более точного перевода
- Увеличить `num_predict` для длинных текстов
- Добавить пост-обработку переведенного текста
- Реализовать перевод по частям для длинных статей

### Этап 4: Улучшение функции очистки
**Задачи:**
- Расширить `clean_html()` для удаления JavaScript и CSS
- Улучшить удаление метаданных RSS
- Сохранять структуру абзацев
- Удалять рекламные блоки

### Этап 5: Тестирование и валидация
**Проверки:**
- Тестирование на реальных RSS-лентах
- Проверка качества перевода
- Валидация сохранения в БД
- Проверка сортировки по дате

---

## 🔧 Технические детали

### Зависимости для добавления
```python
# backend/news-admin/requirements.txt
feedparser==6.0.10
psycopg2-binary==2.9.9
requests==2.31.0
beautifulsoup4==4.12.2
readability-lxml==0.8.1
lxml==4.9.3
```

### Структура БД (уже существует)
```sql
CREATE TABLE news (
    id SERIAL PRIMARY KEY,
    original_title TEXT,
    translated_title TEXT,
    original_excerpt TEXT,
    translated_excerpt TEXT,
    original_content TEXT,      -- ЗДЕСЬ ПРОБЛЕМА
    translated_content TEXT,     -- ЗДЕСЬ ПРОБЛЕМА
    source VARCHAR(255),
    source_url VARCHAR(255),
    link VARCHAR(255) UNIQUE,
    image_url VARCHAR(255),
    category VARCHAR(100),
    published_date TIMESTAMP,
    translated_at TIMESTAMP,
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    is_active BOOLEAN DEFAULT TRUE
);
```

---

## 📊 Метрики успеха

1. **Контент:**
   - ✅ 100% новостей имеют `original_content` > 500 символов
   - ✅ 100% новостей имеют `translated_content` > 500 символов

2. **Качество:**
   - ✅ Перевод читабельный и грамматически корректный
   - ✅ Отсутствуют HTML-теги и метаданные в тексте
   - ✅ Сохранена структура абзацев

3. **Производительность:**
   - ✅ Обработка одной новости < 10 секунд
   - ✅ Обработка ленты (10 новостей) < 2 минут

---

## ⚠️ Ограничения и риски

1. **Web scraping:**
   - Некоторые сайты могут блокировать автоматические запросы
   - Структура HTML может меняться
   - Необходимы User-Agent и обработка ошибок

2. **Ollama:**
   - Требуется локальный сервер на порту 11434
   - Модель llama3.2 должна быть установлена
   - Перевод длинных текстов может быть медленным

3. **RSS-ленты:**
   - Не все ленты предоставляют полный контент
   - Некоторые требуют аутентификацию
   - Могут быть rate limits

---

## 📝 Следующие шаги

1. Изучить промпты для каждого этапа (файлы 01-05)
2. Выполнить исправления поэтапно
3. Тестировать после каждого этапа
4. Документировать изменения
5. Обновить README с инструкциями

---

## 🔗 Связанные файлы

- `backend/news-admin/index.py` - основной файл для исправления
- `backend/news-feed/index.py` - отображение новостей (не требует изменений)
- `src/components/admin/NewsAdmin.tsx` - фронтенд админки
- `src/components/News.tsx` - отображение на сайте

---

**Автор:** AI Architect  
**Версия:** 1.0  
**Статус:** Готов к реализации
