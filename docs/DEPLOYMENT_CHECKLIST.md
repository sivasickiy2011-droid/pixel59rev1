# ✅ Чеклист развёртывания на собственном сервере

Используйте этот чеклист чтобы не пропустить важные шаги при деплое.

---

## 📋 Подготовка сервера

- [ ] Арендован VPS/сервер (минимум 2GB RAM, 20GB SSD)
- [ ] Установлена Ubuntu 22.04 LTS
- [ ] Есть SSH доступ с правами root
- [ ] Зарегистрирован домен (pixel59.ru)
- [ ] Настроены DNS записи:
  - [ ] `pixel59.ru → IP сервера`
  - [ ] `www.pixel59.ru → IP сервера`
  - [ ] `api.pixel59.ru → IP сервера`
  - [ ] `cdn.pixel59.ru → IP сервера`

**Проверка DNS:** `dig pixel59.ru +short` должен показать IP вашего сервера

---

## 🛠 Установка на сервер

- [ ] Запущен скрипт `./setup-server.sh`
- [ ] Docker установлен: `docker --version`
- [ ] Docker Compose установлен: `docker-compose --version`
- [ ] Firewall настроен (порты 22, 80, 443 открыты)

---

## 📦 Настройка проекта

- [ ] Проект склонирован на сервер
- [ ] Создан файл `server/.env` из `server/.env.example`
- [ ] Заполнены обязательные переменные:
  - [ ] `DB_PASSWORD` (пароль PostgreSQL)
  - [ ] `JWT_SECRET` (случайная строка 32+ символа)
  - [ ] `ADMIN_PASSWORD_HASH` (bcrypt хеш вашего пароля)
  - [ ] `S3_ACCESS_KEY` (логин для MinIO)
  - [ ] `S3_SECRET_KEY` (пароль для MinIO, 32+ символа)
- [ ] Заполнены опциональные переменные (если используются):
  - [ ] `YANDEX_METRIKA_TOKEN`
  - [ ] `YANDEX_WEBMASTER_TOKEN`
  - [ ] `TELEGRAM_BOT_TOKEN`
  - [ ] `TELEGRAM_CHAT_ID`
  - [ ] `BITRIX24_WEBHOOK_URL`
- [ ] `CORS_ORIGINS` указаны правильные домены

---

## 🗄 База данных

- [ ] Экспортирована БД из poehali.dev:
  ```bash
  pg_dump ... > pixel_db_export.sql
  ```
- [ ] Дамп загружен на сервер
- [ ] База данных импортирована:
  ```bash
  ./scripts/import-database.sh pixel_db_export.sql
  ```
- [ ] Проверено что данные на месте:
  ```bash
  docker-compose exec postgres psql -U pixel_user -d pixel_db -c "SELECT COUNT(*) FROM portfolio;"
  ```

---

## 🐳 Запуск Docker

- [ ] Все сервисы запущены: `docker-compose up -d`
- [ ] Проверен статус: `docker-compose ps`
- [ ] Все сервисы в состоянии "Up":
  - [ ] `pixel-db` (PostgreSQL)
  - [ ] `pixel-minio` (S3 хранилище)
  - [ ] `pixel-api` (Backend API)
  - [ ] `pixel-nginx` (Web сервер)
  - [ ] `pixel-certbot` (SSL)

---

## 🔒 SSL сертификаты

- [ ] Запущен скрипт: `./scripts/setup-ssl.sh pixel59.ru`
- [ ] Сертификаты получены успешно
- [ ] Nginx перезапущен
- [ ] Проверено что HTTPS работает: `https://pixel59.ru`
- [ ] Проверено что API доступен: `https://api.pixel59.ru/health`

---

## 🎨 Frontend

- [ ] На локальной машине выполнено: `npm run build`
- [ ] Папка `dist/` загружена на сервер:
  ```bash
  scp -r dist/* root@IP:/root/pixel-project/dist/
  ```
- [ ] Сайт открывается в браузере: `https://pixel59.ru`
- [ ] Все страницы работают корректно
- [ ] Изображения загружаются

---

## 📁 MinIO (S3 хранилище)

- [ ] Открыта консоль MinIO: `http://IP:9001`
- [ ] Вход выполнен (логин/пароль из `S3_ACCESS_KEY` / `S3_SECRET_KEY`)
- [ ] Создан bucket: `pixel-files`
- [ ] Установлена политика доступа: `public` (для публичных файлов)
- [ ] Файлы загружены из cdn.poehali.dev
- [ ] Проверено что файлы доступны: `https://cdn.pixel59.ru/pixel-files/test.png`

---

## 🧪 Тестирование

### API тесты:

- [ ] Health check работает:
  ```bash
  curl https://api.pixel59.ru/health
  # Ответ: {"status":"ok",...}
  ```

- [ ] Авторизация работает:
  ```bash
  curl -X POST https://api.pixel59.ru/api/auth/admin \
    -H "Content-Type: application/json" \
    -d '{"password":"ваш_пароль"}'
  # Ответ: {"success":true,"token":"..."}
  ```

- [ ] Портфолио загружается:
  ```bash
  curl https://api.pixel59.ru/api/portfolio
  # Ответ: [{"id":1,...}]
  ```

### Frontend тесты:

- [ ] Главная страница открывается
- [ ] Меню работает
- [ ] Контактная форма отправляется
- [ ] Портфолио загружается
- [ ] Партнёры отображаются
- [ ] Админ-панель доступна по адресу `/admin`

---

## 🔐 Безопасность

- [ ] `.env` файлы НЕ закоммичены в Git
- [ ] Пароли сложные (минимум 16 символов)
- [ ] JWT_SECRET — случайная строка
- [ ] Firewall включен: `sudo ufw status`
- [ ] Только нужные порты открыты (22, 80, 443)
- [ ] SSH вход по ключу (опционально, рекомендуется)

---

## 📊 Мониторинг и логи

- [ ] Логи API проверены: `docker-compose logs api`
- [ ] Логи Nginx проверены: `docker-compose logs nginx`
- [ ] Нет критических ошибок в логах
- [ ] Настроен бэкап базы данных (cron)
- [ ] Мониторинг ресурсов: `docker stats`

---

## 📝 Документация

- [ ] Прочитано `DEPLOY.md`
- [ ] Прочитано `QUICKSTART.md`
- [ ] Сохранены пароли в безопасное место
- [ ] Команда знает как перезапустить сервисы
- [ ] Настроены уведомления о падении сервисов (опционально)

---

## 🎉 Финальная проверка

Откройте сайт в **приватной вкладке** браузера:

- [ ] `https://pixel59.ru` — главная страница загружается
- [ ] Навигация работает (все разделы открываются)
- [ ] Изображения загружаются
- [ ] Контактная форма отправляется
- [ ] Админ-панель работает (`/admin`)
- [ ] Можно залогиниться и управлять контентом

---

## ✅ Всё готово!

Если все пункты отмечены — поздравляю, ваш сайт успешно развёрнут на собственном сервере! 🎉

### Следующие шаги:

1. Настройте автоматический бэкап БД
2. Подключите мониторинг (Grafana + Prometheus)
3. Настройте уведомления о падении сервисов
4. Документируйте процесс для команды

---

## 🆘 Проблемы?

Если что-то пошло не так:

1. Проверьте логи: `docker-compose logs`
2. Перезапустите сервисы: `docker-compose restart`
3. Посмотрите [DEPLOY.md](DEPLOY.md) для подробных инструкций
4. Создайте issue в репозитории

Удачи! 🚀
