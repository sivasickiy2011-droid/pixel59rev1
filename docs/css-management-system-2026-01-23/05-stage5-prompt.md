# Промпт для Этапа 5: Тестирование и документация

**Этап:** 5 из 5 (Финальный)  
**Контекст:** До 200,000 токенов  
**Предыдущие результаты:** `stage1-results.md`, `stage2-results.md`, `stage3-results.md`, `stage4-results.md`

---

## 📋 Контекст проекта

### Что уже реализовано
1. **Этап 1:** Компоненты с BEM-классами
2. **Этап 2:** База данных и Backend API
3. **Этап 3:** Динамический загрузчик CSS
4. **Этап 4:** Админ-панель для управления CSS

### Текущее состояние
Все основные компоненты системы реализованы и требуют финального тестирования, оптимизации и документирования.

---

## 🎯 Задача этапа

Провести комплексное тестирование системы, исправить найденные баги, оптимизировать производительность и создать полную документацию.

### Подзадачи:
1. Функциональное тестирование всех компонентов
2. Регрессионное тестирование существующего функционала
3. Тестирование производительности
4. Тестирование безопасности
5. Создание пользовательской документации
6. Создание технической документации
7. Подготовка к деплою

---

## 🧪 План тестирования

### 1. Функциональное тестирование

#### 1.1 Frontend - BEM классы
```markdown
Чек-лист:
- [ ] Все компоненты имеют BEM-классы
- [ ] Классы консистентны во всех компонентах
- [ ] Визуально ничего не сломалось
- [ ] Адаптивность работает на всех разрешениях
- [ ] Темная тема работает корректно
- [ ] Анимации работают
- [ ] Интерактивные элементы работают

Тестовые сценарии:
1. Открыть каждую секцию сайта
2. Проверить отображение на desktop (1920x1080, 1366x768)
3. Проверить отображение на tablet (768x1024)
4. Проверить отображение на mobile (375x667, 414x896)
5. Переключить темную тему
6. Проверить все анимации
7. Проверить все интерактивные элементы
```

#### 1.2 Backend API
```markdown
Чек-лист:
- [ ] GET /css-blocks работает
- [ ] GET /css-blocks/:id работает
- [ ] GET /css-blocks/by-name/:name работает
- [ ] POST /css-blocks работает
- [ ] PUT /css-blocks/:id работает
- [ ] DELETE /css-blocks/:id работает
- [ ] GET /css-blocks/active работает
- [ ] GET /css-blocks/:id/history работает
- [ ] POST /css-blocks/:id/rollback/:historyId работает
- [ ] Валидация CSS работает
- [ ] Санитизация CSS работает
- [ ] Авторизация проверяется
- [ ] Ошибки обрабатываются корректно

Тестовые запросы (curl/Postman):
```bash
# Получить все блоки
curl -X GET https://pixel59.ru/api/css-blocks \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить активные блоки (без авторизации)
curl -X GET https://pixel59.ru/api/css-blocks/active

# Создать блок
curl -X POST https://pixel59.ru/api/css-blocks \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "block_name": "test-block",
    "component_name": "TestComponent",
    "css_content": ".test { color: red; }",
    "is_active": true,
    "priority": 0,
    "description": "Test block"
  }'

# Обновить блок
curl -X PUT https://pixel59.ru/api/css-blocks/1 \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "css_content": ".test { color: blue; }"
  }'

# Получить историю
curl -X GET https://pixel59.ru/api/css-blocks/1/history \
  -H "Authorization: Bearer YOUR_TOKEN"

# Откатить изменения
curl -X POST https://pixel59.ru/api/css-blocks/1/rollback/5 \
  -H "Authorization: Bearer YOUR_TOKEN"

# Удалить блок
curl -X DELETE https://pixel59.ru/api/css-blocks/1 \
  -H "Authorization: Bearer YOUR_TOKEN"
```
```

#### 1.3 Динамический загрузчик CSS
```markdown
Чек-лист:
- [ ] CSS загружается при старте приложения
- [ ] CSS кэшируется в localStorage
- [ ] CSS применяется без перезагрузки
- [ ] Обновления CSS подхватываются автоматически
- [ ] Fallback на кэш при ошибке работает
- [ ] Производительность приемлема

Тестовые сценарии:
1. Очистить localStorage
2. Открыть сайт
3. Проверить Network tab - запрос к /css-blocks/active
4. Проверить Application tab - наличие кэша
5. Проверить Elements tab - наличие <style id="dynamic-css-styles">
6. Изменить CSS в админ-панели
7. Подождать 5 минут или перезагрузить страницу
8. Проверить применение изменений
9. Отключить интернет
10. Перезагрузить страницу
11. Проверить загрузку из кэша
```

#### 1.4 Админ-панель
```markdown
Чек-лист:
- [ ] Список блоков отображается
- [ ] Поиск работает
- [ ] Фильтрация работает
- [ ] Редактор кода работает
- [ ] Подсветка синтаксиса работает
- [ ] Сохранение работает
- [ ] Превью работает
- [ ] История отображается
- [ ] Откат работает
- [ ] Создание блока работает
- [ ] Удаление блока работает
- [ ] Валидация форм работает
- [ ] Сообщения об ошибках отображаются

Тестовые сценарии:
1. Войти в админ-панель
2. Перейти в раздел "CSS"
3. Проверить отображение списка блоков
4. Использовать поиск
5. Использовать фильтр по компонентам
6. Выбрать блок
7. Отредактировать CSS
8. Сохранить изменения
9. Проверить превью
10. Проверить историю
11. Откатить изменения
12. Создать новый блок
13. Удалить блок
```

### 2. Регрессионное тестирование

```markdown
Чек-лист существующего функционала:
- [ ] Главная страница загружается
- [ ] Навигация работает
- [ ] Все секции отображаются
- [ ] Формы работают (контакты, заказ)
- [ ] Модальные окна работают
- [ ] Админ-панель работает (все разделы)
- [ ] Авторизация работает
- [ ] Партнерский кабинет работает
- [ ] Новости отображаются
- [ ] Портфолио отображается
- [ ] FAQ работает
- [ ] Cookie consent работает
- [ ] Темная тема работает
- [ ] Адаптивность не сломалась
```

### 3. Тестирование производительности

```markdown
Метрики для проверки:

Frontend:
- [ ] First Contentful Paint (FCP) < 1.8s
- [ ] Largest Contentful Paint (LCP) < 2.5s
- [ ] Time to Interactive (TTI) < 3.8s
- [ ] Total Blocking Time (TBT) < 200ms
- [ ] Cumulative Layout Shift (CLS) < 0.1
- [ ] Bundle size увеличение < 50KB

Backend:
- [ ] GET /css-blocks/active < 200ms
- [ ] POST /css-blocks < 500ms
- [ ] PUT /css-blocks/:id < 500ms
- [ ] GET /css-blocks/:id/history < 300ms

Database:
- [ ] Запросы к custom_css_blocks < 50ms
- [ ] Запросы к css_change_history < 100ms
- [ ] Индексы используются эффективно

Инструменты:
- Lighthouse (Chrome DevTools)
- WebPageTest
- GTmetrix
- Chrome DevTools Performance tab
```

### 4. Тестирование безопасности

```markdown
Чек-лист безопасности:

CSS Injection:
- [ ] Валидация CSS на backend работает
- [ ] Санитизация CSS работает
- [ ] Опасные конструкции блокируются
- [ ] XSS через CSS невозможен

API Security:
- [ ] Авторизация проверяется на всех endpoints
- [ ] SQL injection невозможна (параметризованные запросы)
- [ ] Rate limiting настроен
- [ ] CORS настроен правильно

Frontend Security:
- [ ] localStorage не содержит чувствительных данных
- [ ] Токены авторизации защищены
- [ ] CSP headers настроены

Тесты:
1. Попытка внедрить javascript: в CSS
2. Попытка внедрить <script> в CSS
3. Попытка доступа к API без токена
4. Попытка SQL injection в параметрах
5. Попытка XSS через CSS
```

---

## 📚 Документация

### 1. Пользовательская документация

Создать файл `docs/css-management-system-2026-01-23/user-guide.md`:

```markdown
# Руководство пользователя: Управление CSS

## Введение
Система динамического управления CSS позволяет редактировать стили сайта через админ-панель без необходимости пересборки фронтенда.

## Доступ к системе
1. Войдите в админ-панель: https://pixel59.ru/admin/login
2. Перейдите в раздел "CSS" в навигации

## Основные операции

### Просмотр блоков CSS
- Список всех CSS блоков отображается в левой панели
- Используйте поиск для быстрого нахождения блока
- Фильтруйте блоки по компонентам

### Редактирование CSS
1. Выберите блок из списка
2. Отредактируйте CSS в центральной панели
3. Нажмите "Сохранить"
4. Изменения применятся автоматически в течение 5 минут

### Превью изменений
1. Нажмите кнопку "Превью" в редакторе
2. Просмотрите, как будут выглядеть изменения
3. При необходимости внесите корректировки

### История изменений
1. Нажмите кнопку "История" в редакторе
2. Просмотрите все предыдущие версии CSS
3. При необходимости откатите изменения

### Создание нового блока
1. Нажмите кнопку "Создать блок"
2. Заполните форму:
   - Имя блока (например: about-us)
   - Компонент (например: AboutUs)
   - CSS код
   - Приоритет (0-100)
   - Описание
3. Нажмите "Создать"

### Удаление блока
1. Выберите блок из списка
2. Нажмите кнопку удаления
3. Подтвердите действие

## Лучшие практики

### Именование блоков
- Используйте kebab-case: about-us, hero-section
- Имя должно соответствовать компоненту
- Избегайте слишком длинных имен

### Написание CSS
- Используйте BEM-классы компонентов
- Избегайте !important
- Тестируйте на разных разрешениях
- Проверяйте темную тему

### Приоритет блоков
- 0 - обычный приоритет
- 50 - средний приоритет
- 100 - высокий приоритет
- Блоки с высоким приоритетом применяются последними

## Устранение проблем

### CSS не применяется
1. Проверьте, что блок активен
2. Очистите кэш браузера
3. Подождите 5 минут для автообновления
4. Проверьте консоль браузера на ошибки

### Ошибка валидации CSS
1. Проверьте синтаксис CSS
2. Убедитесь, что нет опасных конструкций
3. Используйте CSS валидатор

### Изменения не сохраняются
1. Проверьте подключение к интернету
2. Проверьте авторизацию
3. Обратитесь к администратору
```

### 2. Техническая документация

Создать файл `docs/css-management-system-2026-01-23/technical-guide.md`:

```markdown
# Техническая документация: Система управления CSS

## Архитектура

### Компоненты системы
1. **Frontend (React + TypeScript)**
   - DynamicCSSLoader - загрузчик CSS
   - useDynamicCSS - хук управления
   - CSSManagement - админ-панель

2. **Backend (Python)**
   - API endpoints для CRUD операций
   - Валидация и санитизация CSS
   - История изменений

3. **Database (PostgreSQL)**
   - custom_css_blocks - хранение блоков
   - css_change_history - история изменений

### Поток данных
```
Admin Panel → Backend API → PostgreSQL
                ↓
Frontend ← DynamicCSSLoader ← Backend API
```

## API Reference

### Endpoints

#### GET /css-blocks
Получить все CSS блоки

**Query Parameters:**
- `component` (string, optional) - фильтр по компоненту
- `active` (boolean, optional) - фильтр по активности

**Response:**
```json
{
  "blocks": [
    {
      "id": 1,
      "block_name": "about-us",
      "component_name": "AboutUs",
      "css_content": ".about-us { ... }",
      "is_active": true,
      "priority": 0,
      "created_at": "2026-01-23T10:00:00Z",
      "updated_at": "2026-01-23T10:00:00Z",
      "created_by": "admin",
      "description": "About Us section styles"
    }
  ],
  "count": 1
}
```

[... документация для всех endpoints ...]

## Deployment

### Требования
- Node.js 18+
- Python 3.9+
- PostgreSQL 14+

### Установка

1. **База данных**
```bash
psql -U pixel_user -d pixel_db -f db_migrations/2026-01-23_css_management.sql
```

2. **Backend**
```bash
cd backend/css-management
pip install -r requirements.txt
# Deploy serverless function
```

3. **Frontend**
```bash
npm install
npm run build
# Deploy to production
```

### Конфигурация

**Environment Variables:**
```env
VITE_API_URL=https://pixel59.ru/api
DATABASE_URL=postgresql://pixel_user:password@localhost:5432/pixel_db
```

## Мониторинг

### Метрики для отслеживания
- Количество запросов к /css-blocks/active
- Время ответа API
- Размер кэша CSS
- Частота обновлений CSS
- Ошибки валидации

### Логирование
- Backend логи: `/var/log/css-management/`
- Frontend логи: Browser Console
- Database логи: PostgreSQL logs

## Troubleshooting

### Проблема: CSS не загружается
**Симптомы:** Стили не применяются на фронтенде

**Решение:**
1. Проверить доступность API: `curl https://pixel59.ru/api/css-blocks/active`
2. Проверить логи backend
3. Проверить базу данных: `SELECT * FROM custom_css_blocks WHERE is_active = true`
4. Очистить кэш: `localStorage.clear()`

### Проблема: Медленная загрузка
**Симптомы:** Долгое время загрузки CSS

**Решение:**
1. Проверить размер CSS блоков
2. Оптимизировать CSS (минификация)
3. Проверить индексы в БД
4. Увеличить время кэширования

[... другие проблемы и решения ...]
```

### 3. API документация

Создать файл `docs/css-management-system-2026-01-23/api-documentation.md` с полным описанием всех endpoints, примерами запросов и ответов.

---

## 🚀 Подготовка к деплою

### Чек-лист перед деплоем

```markdown
Backend:
- [ ] Миграции БД протестированы
- [ ] API endpoints протестированы
- [ ] Валидация работает
- [ ] Авторизация настроена
- [ ] Логирование настроено
- [ ] Error handling настроен
- [ ] Environment variables настроены

Frontend:
- [ ] Build проходит без ошибок
- [ ] Lint проходит без ошибок
- [ ] Bundle size приемлем
- [ ] Environment variables настроены
- [ ] API URL настроен правильно

Database:
- [ ] Backup создан
- [ ] Миграции готовы
- [ ] Индексы созданы
- [ ] Permissions настроены

Documentation:
- [ ] Пользовательская документация готова
- [ ] Техническая документация готова
- [ ] API документация готова
- [ ] Changelog обновлен
```

### План деплоя

```markdown
1. **Подготовка (за 1 день)**
   - Создать backup БД
   - Создать ветку release
   - Финальное тестирование

2. **Деплой Backend (30 минут)**
   - Применить миграции БД
   - Deploy serverless functions
   - Проверить API endpoints

3. **Деплой Frontend (30 минут)**
   - Build production
   - Deploy to server
   - Проверить загрузку сайта

4. **Проверка (1 час)**
   - Smoke testing
   - Проверка всех функций
   - Мониторинг ошибок

5. **Откат (если нужно)**
   - Откат миграций БД
   - Откат к предыдущей версии кода
   - Очистка кэша
```

---

## 📄 Формат финального отчета

Создайте файл `stage5-results.md`:

```markdown
# Результаты Этапа 5: Тестирование и документация

## Тестирование

### Функциональное тестирование
- ✅ Frontend BEM классы: 100% пройдено
- ✅ Backend API: 100% пройдено
- ✅ Динамический загрузчик: 100% пройдено
- ✅ Админ-панель: 100% пройдено

### Регрессионное тестирование
- ✅ Существующий функционал: Без регрессий
- ✅ Все секции работают
- ✅ Формы работают
- ✅ Админ-панель работает

### Тестирование производительности
- ✅ FCP: 1.2s (цель < 1.8s)
- ✅ LCP: 2.1s (цель < 2.5s)
- ✅ TTI: 3.2s (цель < 3.8s)
- ✅ Bundle size: +12KB (цель < 50KB)

### Тестирование безопасности
- ✅ CSS injection защита работает
- ✅ API авторизация работает
- ✅ XSS защита работает
- ✅ SQL injection защита работает

## Документация

### Созданные документы
- ✅ user-guide.md - Руководство пользователя
- ✅ technical-guide.md - Техническая документация
- ✅ api-documentation.md - API документация
- ✅ deployment-guide.md - Руководство по деплою
- ✅ troubleshooting.md - Устранение проблем

## Найденные и исправленные баги
1. Bug #1: CSS не применялся при первой загрузке
   - Причина: Race condition в useDynamicCSS
   - Решение: Добавлен useEffect с правильными зависимостями

2. Bug #2: История изменений не обновлялась
   - Причина: Триггер в БД не срабатывал
   - Решение: Исправлен SQL триггер

[... другие баги ...]

## Оптимизации
1. Минификация CSS перед сохранением
2. Добавлено сжатие gzip для API ответов
3. Оптимизированы SQL запросы с индексами
4. Добавлено lazy loading для Monaco Editor

## Готовность к деплою
- ✅ Все тесты пройдены
- ✅ Документация готова
- ✅ Backup БД создан
- ✅ План деплоя готов
- ✅ Rollback план готов

## Рекомендации
1. Мониторить производительность первые 24 часа
2. Собирать feedback от пользователей
3. Планировать оптимизации на основе метрик
4. Рассмотреть добавление A/B тестирования CSS

## Заключение
Система динамического управления CSS полностью готова к продакшену. Все компоненты протестированы, документация создана, производительность оптимизирована.
```

---

## ✅ Критерии завершения проекта

- [ ] Все 5 этапов завершены
- [ ] Все тесты пройдены
- [ ] Документация создана
- [ ] Производительность оптимизирована
- [ ] Безопасность проверена
- [ ] Готовность к деплою подтверждена
- [ ] Команда обучена работе с системой

---

**Статус:** 🚀 Готов к выполнению  
**Это финальный этап проекта**  
**После завершения:** Система готова к продакшену
