# 🔧 Оставшиеся API endpoints для конвертации

Я создал базовую инфраструктуру и основные API endpoints. Ниже список функций, которые нужно доконвертировать из cloud functions в Express.js API.

---

## ✅ Уже готово (3 из 30)

1. ✅ `auth-admin` → `/api/auth/admin`
2. ✅ `contact-form` → `/api/contact`
3. ✅ `portfolio` → `/api/portfolio`

---

## 📝 Нужно доконвертировать (27 функций)

### Высокий приоритет (основной функционал)

1. **partners** → `/api/partners`
   - Файл: `backend/partners/index.py`
   - Что делает: Управление партнёрами
   - Используется: На странице партнёров

2. **partner-logos** → `/api/partners/logos`
   - Файл: `backend/partner-logos/index.py`
   - Что делает: Получение логотипов партнёров
   - Используется: На главной странице

3. **services-admin** → `/api/services`
   - Файл: `backend/services-admin/index.py`
   - Что делает: Управление услугами
   - Используется: Админ-панель услуг

4. **submit-order** → `/api/orders`
   - Файл: `backend/submit-order/index.py`
   - Что делает: Приём заказов
   - Используется: Форма заказа услуг

5. **secure-settings** → `/api/settings`
   - Файл: `backend/secure-settings/index.py`
   - Что делает: Управление настройками (хранилище секретов)
   - Используется: Админ-панель настроек

### Средний приоритет (дополнительный функционал)

6. **partner-auth** → `/api/auth/partner`
   - Файл: `backend/partner-auth/index.py`
   - Что делает: Авторизация партнёров
   - Используется: Личный кабинет партнёров

7. **upload-logo** → `/api/upload/logo`
   - Файл: `backend/upload-logo/index.py`
   - Что делает: Загрузка логотипов
   - Используется: Админ-панель

8. **upload-partner-logo** → `/api/upload/partner-logo`
   - Файл: `backend/upload-partner-logo/index.py`
   - Что делает: Загрузка логотипов партнёров
   - Используется: Админ-панель партнёров

9. **change-admin-password** → `/api/auth/change-password`
   - Файл: `backend/change-admin-password/index.py`
   - Что делает: Смена пароля администратора
   - Используется: Админ-панель

10. **admin-login-logs** → `/api/auth/admin/logs`
    - Файл: `backend/admin-login-logs/index.py`
    - Что делает: История входов администратора
    - Используется: Админ-панель безопасности

### Аналитика и SEO

11. **yandex-metrika-stats** → `/api/analytics/metrika`
    - Файл: `backend/yandex-metrika-stats/index.py`
    - Что делает: Статистика Яндекс.Метрики
    - Используется: Админ-панель аналитики

12. **yandex-webmaster-issues** → `/api/analytics/webmaster`
    - Файл: `backend/yandex-webmaster-issues/index.py`
    - Что делает: Проблемы из Вебмастера
    - Используется: Админ-панель аналитики

13. **seo-analyze** → `/api/seo/analyze`
    - Файл: `backend/seo-analyze/index.py`
    - Что делает: SEO анализ страниц
    - Используется: Админ-панель SEO

14. **seo-apply** → `/api/seo/apply`
    - Файл: `backend/seo-apply/index.py`
    - Что делает: Применение SEO рекомендаций
    - Используется: Админ-панель SEO

15. **seo-batch-analyze** → `/api/seo/batch`
    - Файл: `backend/seo-batch-analyze/index.py`
    - Что делает: Массовый SEO анализ
    - Используется: Админ-панель SEO

### Служебные функции

16. **bot-logger** → `/api/bot/log`
    - Файл: `backend/bot-logger/index.py`
    - Что делает: Логирование активности ботов
    - Используется: Защита от ботов

17. **bot-stats** → `/api/bot/stats`
    - Файл: `backend/bot-stats/index.py`
    - Что делает: Статистика ботов
    - Используется: Админ-панель безопасности

18. **consent** → `/api/consent`
    - Файл: `backend/consent/index.py`
    - Что делает: Управление согласиями пользователей (cookies)
    - Используется: Cookie consent баннер

19. **news-feed** → `/api/news`
    - Файл: `backend/news-feed/index.py`
    - Что делает: Лента новостей
    - Используется: Страница новостей

### Утилиты и вспомогательные

20. **convert-yandex-disk-url** → `/api/utils/convert-disk-url`
    - Файл: `backend/convert-yandex-disk-url/index.py`
    - Что делает: Конвертация ссылок Яндекс.Диска
    - Используется: Вспомогательная утилита

21. **check-s3-file** → `/api/utils/check-s3`
    - Файл: `backend/check-s3-file/index.py`
    - Что делает: Проверка файлов в S3
    - Используется: Диагностика

22. **fix-s3-permissions** → `/api/utils/fix-s3-permissions`
    - Файл: `backend/fix-s3-permissions/index.py`
    - Что делает: Исправление прав доступа S3
    - Используется: Утилита обслуживания

23. **get-partner-logos-public** → `/api/partners/logos/public`
    - Файл: `backend/get-partner-logos-public/index.py`
    - Что делает: Публичные логотипы партнёров
    - Используется: Публичная страница

24. **admin-partner-logos** → `/api/admin/partner-logos`
    - Файл: `backend/admin-partner-logos/index.py`
    - Что делает: Админ управление логотипами
    - Используется: Админ-панель

### Специальные функции для poehali.dev (можно пропустить)

25. **copy-secrets-to-db** → Не нужна для self-hosted
    - Использовалась только для миграции на poehali.dev

26. **clear-secrets-cache** → Не нужна для self-hosted
    - Использовалась для очистки кеша на платформе

27. **webapp** → Это не функция, а папка с web приложением

---

## 🎯 План конвертации

### Фаза 1: Критичный функционал (первые 5 функций)
Без этих функций сайт не будет работать полностью.

### Фаза 2: Дополнительный функционал (функции 6-10)
Улучшают UX, но сайт работает и без них.

### Фаза 3: Аналитика и SEO (функции 11-15)
Для маркетинга и SEO оптимизации.

### Фаза 4: Служебные функции (функции 16-24)
Для полного функционала админ-панели.

---

## 📖 Как конвертировать функцию

### Шаблон для конвертации:

1. **Откройте файл функции:**
   ```bash
   cat backend/partner-logos/index.py
   ```

2. **Создайте Express.js роутер:**
   ```javascript
   // server/api/partners.js
   const express = require('express');
   const router = express.Router();
   const pool = require('../config/database');
   
   router.get('/logos', async (req, res) => {
     // Конвертируйте логику из Python
     try {
       const result = await pool.query('SELECT * FROM partner_logos');
       res.json(result.rows);
     } catch (error) {
       console.error('Error:', error);
       res.status(500).json({ error: 'Failed to fetch logos' });
     }
   });
   
   module.exports = router;
   ```

3. **Добавьте в server.js:**
   ```javascript
   app.use('/api/partners', require('./api/partners'));
   ```

4. **Обновите frontend:**
   ```typescript
   // src/config/api.ts
   partners: {
     logos: `${API_BASE_URL}/partners/logos`,
   }
   ```

---

## 🤖 Автоматизация

Можно использовать AI (ChatGPT, Claude) для конвертации:

**Промпт:**
```
Конвертируй эту Python cloud function в Express.js endpoint:

[вставьте код из backend/FUNCTION_NAME/index.py]

Требования:
- Используй async/await
- PostgreSQL через pool.query()
- Обработка ошибок через try/catch
- CORS headers
- Валидация входных данных
```

---

## 💡 Советы

1. **Начните с простых функций** (GET запросы без сложной логики)
2. **Тестируйте каждую функцию** после конвертации
3. **Используйте одинаковые названия** endpoints как в func2url.json
4. **Сохраняйте структуру ответов** как в оригинальных функциях

---

## 📞 Нужна помощь?

Если застряли на конвертации:
1. Посмотрите примеры в `server/api/auth.js` и `server/api/contact.js`
2. Прочитайте [Express.js документацию](https://expressjs.com/)
3. Используйте AI ассистентов для конвертации кода

Удачи! 🚀
